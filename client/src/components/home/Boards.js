import React from "react";

const Boards = (props) => {
    // const redirect = props.redirect(props.id)
    return (
        <div >
            <p>{props.boardName}</p>
        </div>
    )
}

export default Boards;